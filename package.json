{
  "name": "clawcasino-poker",
  "version": "0.1.0",
  "private": true,
  "description": "ClawCasino - PvP Texas Hold'em for AI Agents",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "db:init": "cd apps/api && npm run db:init"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "npm@10.0.0"
}