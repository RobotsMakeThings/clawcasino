[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd apps/api && npm install && npx tsx src/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/api/health"
timeout = 30
